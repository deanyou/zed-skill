name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build:
    name: Build skill-lsp (${{ matrix.target }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            archive: skill-lsp-x86_64-linux.tar.gz
            bin: skill-lsp
          - target: x86_64-apple-darwin
            os: macos-latest
            archive: skill-lsp-x86_64-macos.tar.gz
            bin: skill-lsp
          - target: aarch64-apple-darwin
            os: macos-latest
            archive: skill-lsp-aarch64-macos.tar.gz
            bin: skill-lsp
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            archive: skill-lsp-x86_64-windows.zip
            bin: skill-lsp.exe
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: skill-lsp
      - name: Build
        run: cargo build --release --target ${{ matrix.target }}
        working-directory: skill-lsp
      - name: Package (unix)
        if: runner.os != 'Windows'
        run: |
          cp skill-lsp/target/${{ matrix.target }}/release/${{ matrix.bin }} ${{ matrix.bin }}
          tar czf ${{ matrix.archive }} ${{ matrix.bin }}
          sha256sum ${{ matrix.archive }} > ${{ matrix.archive }}.sha256
      - name: Package (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Copy-Item skill-lsp/target/${{ matrix.target }}/release/${{ matrix.bin }} ${{ matrix.bin }}
          Compress-Archive -Path ${{ matrix.bin }} -DestinationPath ${{ matrix.archive }}
          (Get-FileHash ${{ matrix.archive }} -Algorithm SHA256).Hash.ToLower() + "  ${{ matrix.archive }}" | Out-File -Encoding ascii ${{ matrix.archive }}.sha256
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.archive }}
          path: |
            ${{ matrix.archive }}
            ${{ matrix.archive }}.sha256

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            skill-lsp-*.tar.gz
            skill-lsp-*.zip
            *.sha256
          generate_release_notes: true
